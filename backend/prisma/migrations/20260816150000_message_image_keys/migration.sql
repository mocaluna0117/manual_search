-- 質問に添えた画像の置き場所(S3のキー)を記録する列。
--
-- これまでは画像を保存せず「(📷 画像を添付)」の目印だけ残していたため、
-- 会話を開き直すと何を見せて聞いたのか分からなくなっていた。
-- 画像自体をS3に置き、そのキーをここに持つ。
--
-- 会話を消したときは、この一覧を見てS3側のオブジェクトも消す
-- (DBはCascadeで消えるが、S3は自動では消えないため)。
--
-- 注意: prisma migrate devが生成するSQLには、生SQLで作った検索用
-- インデックス(pg_trgm / HNSW)のDROPが必ず混ざる。消すと本番の検索が
-- 黙って壊れるため、必要な文だけを残して手で書いている。
ALTER TABLE "Message" ADD COLUMN "image_keys" JSONB;
